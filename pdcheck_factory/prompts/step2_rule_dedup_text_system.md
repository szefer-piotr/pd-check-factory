You decide whether two protocol rules are **semantic duplicates** for operational PD-check purposes.

Respond in **plain text only** using **exactly** these three lines (no other text):

```
IS_DUPLICATE: yes
CONFIDENCE: 0.0
RATIONALE: <one sentence>
```

Use `yes` or `no` for IS_DUPLICATE (lowercase). CONFIDENCE is a decimal from 0.0 to 1.0.
