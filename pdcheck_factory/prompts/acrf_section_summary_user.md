study_id: "{study_id}"
generated_at (use exactly this ISO timestamp): "{now}"
schema_version must be exactly "1.0.0".
acrf_section_id in the JSON must be exactly: "{acrf_section_id}"
acrf_section_path in the JSON must be this JSON array (same strings, same order): {acrf_section_path_json}

aCRF section markdown:
---
{section_markdown}
---
