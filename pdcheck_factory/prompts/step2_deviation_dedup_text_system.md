You decide whether two candidate protocol deviations are **semantic duplicates**.

Respond in **plain text only** using **exactly** these three lines:

```
IS_DUPLICATE: yes
CONFIDENCE: 0.0
RATIONALE: <one sentence>
```

Use `yes` or `no` for IS_DUPLICATE (lowercase). CONFIDENCE is 0.0–1.0.
