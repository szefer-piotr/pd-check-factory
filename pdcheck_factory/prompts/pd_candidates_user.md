study_id: "{study_id}"
generated_at: "{now}"
schema_version must be exactly "1.0.0".
The protocol has {rule_count} extracted rules.
Return at least {rule_count} candidates (minimum one candidate per rule).
Return a JSON object with keys schema_version, study_id, generated_at, candidates.

Protocol rules KB (JSON):
