study_id: {study_id}
deviation_id: {deviation_id}
protocol_deviation_category: {protocol_deviation_category}
protocol_deviation_sub_category: {protocol_deviation_sub_category}
classification: {classification}

Current deviation text:
{deviation_text}

Existing pseudo logic seed (if any):
{pseudo_logic_seed}

Protocol paragraph candidates (cite only these ids):
{paragraph_candidates}

Merged aCRF summary:
{acrf_summary}
