study_id: {study_id}
deviation_id: {deviation_id}
protocol_deviation_category: {protocol_deviation_category}
protocol_deviation_sub_category: {protocol_deviation_sub_category}
classification: {classification}

Imported deviation text:
{deviation_text}

Full paragraph-numbered protocol (cite only these paragraph ids):
{protocol_paragraphs}
