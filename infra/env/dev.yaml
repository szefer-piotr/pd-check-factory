project:
  name: pd-check-factory
  prefix: pdchk
  environment: dev
  location: westeurope
  region_short: weu
  unique_suffix: "01"

naming:
  resource_group: "rg-{prefix}-{environment}-{region_short}"
  storage_account: "st{prefix}{environment}{region_short}{unique_suffix}"
  key_vault: "kv-{prefix}-{environment}-{region_short}"
  app_insights: "ai-{prefix}-{environment}-{region_short}"
  app_service: "app-{prefix}-{environment}-{region_short}"
  function_app: "func-{prefix}-{environment}-{region_short}"
  ai_search: "srch-{prefix}-{environment}-{region_short}"
  openai: "aoai-{prefix}-{environment}-{region_short}"

tags:
  project: pd-check-factory
  env: dev
  region: westeurope
  owner: bioscope
  managedBy: bicep
  costCenter: innovation
  dataSensitivity: internal
